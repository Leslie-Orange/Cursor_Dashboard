---
description: 凡有文件交付，必须把最终文件链接发到聊天里
alwaysApply: true
---

# 交付文件必须发到聊天

只要本轮产出了用户可拿走的最终文件（安装包、图片、导出件、构建产物、报告、压缩包等），回复里必须给出可点击链接，不要只写路径。

- 优先用工作区相对路径的 Markdown 链接，例如 `[Cursor额度仪表盘-1.0.0.dmg](dist/Cursor额度仪表盘-1.0.0.dmg)`
- 同时可用 `open_resource` 在编辑器中打开该文件
- 多个交付物就逐条列出，每条都带链接
- 说明文字可以有，但不能替代链接
